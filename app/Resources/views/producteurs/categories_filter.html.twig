
<p>Filtrer par catégorie</p>
{% for category in categories %}
    <div class=" col-md-6">
        <a href="{{ path('producteurs', { 'category': category.libelle }) }}">
            <img class="banniere_img img-responsive" src="{{ asset( category.logo) }}" alt="{{ category.libelle}}" />
        </a>
    </div>
{% endfor %}