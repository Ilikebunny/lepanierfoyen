{% extends 'layout.html.twig' %}

{% block title %}
    {{ parent() }} - producteur
{% endblock %}

{% block panel_head %}
    {% include 'producteurs/Panel_producteur.html.twig' %}
{% endblock %}

{% block menu_content %}
    {% include 'producteurs/navTab.html.twig' %}
{% endblock %}

{% block aside %}
    {{ include('producteurs/aside.html.twig') }}
{% endblock %}

{% block body %}

  {#  <section class="row">
        {% for gallery in producteur.myGallery %}
            <div class="col-xs-10 col-sm-8 col-md-6">
                <img class="gallery_img img-responsive" src="{{ asset( gallery.image) }}" alt="{{ gallery.id}}" />
            </div>
        {% endfor %}
    </section>
#}
    {% set myVal = 0 %}

    <section class="row">
        <div id="myCarousel" class="carousel slide" data-ride="carousel">
            <!-- Indicators -->
            <ol class="carousel-indicators">
                {% for gallery in producteur.myGallery %}
                    {% if myVal == 0 %}
                        <li data-target="#myCarousel" data-slide-to="{{ myVal }}" class="active"></li>
                        {% else %}
                        <li data-target="#myCarousel" data-slide-to="{{ myVal }}"></li>
                        {% endif %}
                        {% set myVal = myVal + 1 %}
                    {% endfor %}
            </ol>

            <!-- Wrapper for slides -->
            {% set myVal = 0 %}
            <div class="carousel-inner" role="listbox">

                {% for gallery in producteur.myGallery %}
                    {% if myVal == 0 %}
                        <div class="item active">
                        {% else %}
                            <div class="item">
                            {% endif %}
                            <img class="center-block" src="{{ asset( gallery.image) }}" alt="{{ gallery.id}}">
                        </div>
                        {% set myVal = myVal + 1 %}
                    {% endfor %}

                </div>

                <!-- Left and right controls -->
                <a class="left carousel-control" href="#myCarousel" role="button" data-slide="prev">
                    <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
                    <span class="sr-only">Previous</span>
                </a>
                <a class="right carousel-control" href="#myCarousel" role="button" data-slide="next">
                    <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
                    <span class="sr-only">Next</span>
                </a>
            </div>
    </section>

{% endblock %}

