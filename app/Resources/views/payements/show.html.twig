{% extends 'layout.html.twig' %}

{% block title %}
{{ parent() }} - payement
{% endblock %}

{% block body %}

<header class="panel-heading">
    <h2> Payements</h2>
</header>
<div class="panel-body">

    <form class="form form-vertical">
        <fieldset>            <div class="control-group">
                <label class="col-sm-2 control-label">Id</label>
                <div class="col-sm-10">
                    <span class="input-xlarge uneditable-input">{{ payement.id }}</span>
                </div>
            </div>            <div class="control-group">
                <label class="col-sm-2 control-label">Moyen</label>
                <div class="col-sm-10">
                    <span class="input-xlarge uneditable-input">{{ payement.moyen }}</span>
                </div>
            </div>            <div class="control-group">
                <label class="col-sm-2 control-label">Qte</label>
                <div class="col-sm-10">
                    <span class="input-xlarge uneditable-input">{{ payement.qte }}</span>
                </div>
            </div>            <div class="control-group">
                <label class="col-sm-2 control-label">Montant</label>
                <div class="col-sm-10">
                    <span class="input-xlarge uneditable-input">{{ payement.montant }}</span>
                </div>
            </div>        </fieldset>
    </form>
        <hr>
        <div class="form-group">
    <a class="btn btn-default" href="{{ path('payements') }}">
        <i class="fa fa-list" aria-hidden="true"></i>
        Back to list
    </a>
            <a class="btn btn-success" href="{{ path('payements_edit', { 'id':  payement.id }) }}">
            <i class="fa fa-pencil" aria-hidden="true"></i>
            Edit
        </a>
                <a class="btn btn-primary" href="{{ path('payements_new') }}">
            <i class="fa fa-plus" aria-hidden="true"></i>
            New
        </a>
                <form action="{{ path('payements_delete', { 'id': payement.id }) }}" method="post" style="display: inline-block">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button class="btn btn-danger" type="submit" onclick="return confirm('Are you sure');">
                <i class="fa fa-trash-o"></i> 
                Delete</button>
        </form>
    </div>

</div>
<hr>

{% endblock %}

