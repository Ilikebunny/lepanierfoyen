
<p>Filtrer par tag</p>
{% for tag in tags %}
    <div class=" col-md-6">
        <a href="{{ path('articlesByTag', { 'tagLibelle': tag.title }) }}">
            <span class="label label-default">{{ tag.title}}</span>
        </a>
    </div>
{% endfor %}